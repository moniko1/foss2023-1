#include <stdio.h>

int main()
{
	printf("Hello World 201610002\n");  // Print

	return 0;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:\a%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
#include <stdio.h>

int main()
{
	printf("Hello World 201610002\n");  // Print

	return 0;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:\a%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
#include <stdio.h>

int main()
{
	printf("Hello World 201610002\n");  // Print

	return 0;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:\a%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
#include <stdio.h>

int main()
{
	printf("Hello World 201610002\n");  // Print

	return 0;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:\a%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
