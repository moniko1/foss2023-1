#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
#include <stdio.h>

int main()
{
	int a;
	fscanf(stdin, "%d",&a);
	fprintf(stdout,"out:%d\n",a);  
	fprintf(stderr, "err:%d\n",a);
	return a;
}
